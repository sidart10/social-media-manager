# Generate Cinematic Sequence from JSON
name: generate-cinematic-sequence
description: "Create cinematic video using JSON prompts → Images → Animation → Merge"
author: "AI Video Agent with JSON Prompt Generator Module"

# Variables
json_module: "{project-root}/bmad/modules/json-prompt-generator"
sequence_template: "{json_module}/templates/video-sequence.json"
conversion_workflow: "{json_module}/workflows/convert-json-to-text.yaml"
output_folder: "{agent-folder}/ai-video-agent-sidecar/outputs"

# This is an action workflow
template: false
instructions: "{installed_path}/cinematic-sequence-instructions.md"
