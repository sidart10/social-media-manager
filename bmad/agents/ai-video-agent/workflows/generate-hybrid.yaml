# Generate Hybrid Mixed-Media Video Workflow
name: generate-hybrid
description: "Mix talking head + generated scenes + images into one video"
author: "BMad AI Video Agent"

config_source: "{agent-folder}/ai-video-agent-sidecar/config.yaml"
output_folder: "{agent-folder}/ai-video-agent-sidecar/outputs"
date: system-generated

template: false
instructions: "{installed_path}/hybrid-instructions.md"
default_output_file: "{output_folder}/hybrid_{date}.mp4"
